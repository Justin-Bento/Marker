---
import Card from '../components/Card.astro';
import Appbar from '../components/Appbar.astro';
import Footer from '../components/Footer.astro';
const allPosts = await Astro.glob('../pages/posts/*.md');
---

<html lang="en">
  <head>
    <title>Marker</title>
  </head>
  <body class="container my-8 lg:my-12">
    <Appbar />
    <main class="space-y-6 my-6">
      <section class="leading-7 text-gray-700 tracking-wide@">
        <p>
          Our platform's goal is to help people with epilepsy discover the benefits of keeping a seizure diary. As you explore these pages, you'll uncover
          a personal way to track seizures, identify triggers, and refine your treatment plans. Over time, this activity will foster a
          deeper understanding of your triggers, relationship with epilepsy, and empower you to manage your condition better. 
      </section>
      <hr />
      <section class="space-y-8 my-8">
        {allPosts.map((post) => <Card href={post.url} title={post.frontmatter.title} body={post.frontmatter.description} />)}
      </section>
    </main>
    <Footer />
  </body>

  <script>
    // JavaScript code goes here.
  </script>

  <style is:global>
    @font-face {
      font-family: 'Golos Text';
      src: url('/fonts/GolosText-VariableFont_wght.ttf') format('truetype');
      font-weight: 100 900;
      font-style: normal;
      font-display: swap;
    }
    * {
      font-family: 'Golos Text', sans-serif;
    }
  </style>
</html>
